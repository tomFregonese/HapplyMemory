import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HapplyMemoryIonicMigration',
  webDir: 'dist'
};

export default config;
